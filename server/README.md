# Server

Express process for XRP Insights. See the [root README](../README.md) for local run, Render, and API docs.

```bash
cd server
npm install
npm start
```

Serves `/api/*` and `public/` on the same origin.
